Classes that were created to perform an analysis on a Hanbury-Brown-Twiss experiment with X-rays using images produced by a CCD sensor. 

- the class `ImageAnalysis` performed different analysis on an image  
- the class `ImageAnalysisGroup` uses the previous class and perform similar analysis in a set of images  

